apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
spec:
  type: LoadBalancer
  selector:
    purpose: ebs-static
    purpose: ebs-dynamic
    project: expense
  ports:
  - protocol: TCP
    port: 80 #service-port;
    targetPort: 80