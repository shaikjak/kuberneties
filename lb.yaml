apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
  namespace: expense-dev
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80        # Service port
      targetPort: 80 # Pod port
